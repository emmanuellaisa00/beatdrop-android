name: Build & Deploy BeatDrop

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant gradlew permission
        run: chmod +x ./gradlew

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*','**/gradle-wrapper.properties') }}
          restore-keys: gradle-${{ runner.os }}-

      - name: Build Android-13-compatible release APK
        run: ./gradlew assembleRelease --stacktrace

      - name: Verify APK
        run: |
          APK=$(ls app/build/outputs/apk/release/*.apk | head -1)
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs "$APK"
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging "$APK" | head -30

      - name: Gather single APK
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/release/*.apk dist/BeatDrop-Android13.apk
          ls -lh dist
          sha256sum dist/BeatDrop-Android13.apk > dist/BeatDrop-Android13.apk.sha256
          cat dist/BeatDrop-Android13.apk.sha256

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: BeatDrop-apk
          path: dist/BeatDrop-Android13.apk
          if-no-files-found: error

      - name: Publish GitHub Release
        if: github.event_name != 'pull_request'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: BeatDrop Android 13 compatible build ${{ github.run_number }}
          body: |
            BeatDrop Android 13 compatible APK — build ${{ github.run_number }}
            Commit: ${{ github.sha }}

            This build targets Android 13 (targetSdk 33) and removes API-34-only foreground-service permissions to avoid package-parser issues on Android 13 installers.
          files: |
            dist/BeatDrop-Android13.apk
            dist/BeatDrop-Android13.apk.sha256
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
